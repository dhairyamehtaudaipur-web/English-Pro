{
  "name": "englishpro-ai",
  "version": "1.0.0",
  "description": "EnglishPro AI Premium Grammar App",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp -r assets www/ 2>/dev/null || :",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/assets": "^3.0.0"
  }
}
